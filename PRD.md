# PRD · 个人工作生活工作台

| 项目 | 内容 |
|---|---|
| 产品暂定名 | 工作台（可改，只在一个地方改，全局生效） |
| 使用者 | 小蝶，单人使用，不需要多人协作 |
| 文档版本 | v1.0 |
| 日期 | 2026-09-15 |
| 状态 | 需求已确认，作为第一版开发依据 |
| 运行环境 | 本机 macOS + 本机浏览器，不联网、不部署、不做手机端 |

---

## 1. 产品目标和使用场景

### 1.1 目标

做一个**自己每天真的会打开**的私人工作台，把散落在备忘录、Excel、脑子里的事情收进一个地方。

三件事要做好，其他都可以往后放：

1. **一眼看现状**——早上打开首页，今天该干什么、各摊子什么状态，十秒内看完。
2. **随手能记**——灵感、备忘、待跟进，记下来不费劲，不需要先想"这该放哪个模块"。
3. **数据在自己手里**——数据是本机一个能看懂的文本文件，能备份、能搬走、不依赖任何服务。

### 1.2 使用场景

| 场景 | 触发 | 期望 |
|---|---|---|
| 早上开工 | 双击启动，打开首页 | 看到今日重点、今天的任务、以及各模块昨天到今天的变化 |
| 干活中途冒出一个念头 | `Cmd + K` | 两三秒记下来，不打断手上的活 |
| 下午做自媒体 | 进自媒体模块 | 知道选题卡在哪一步、哪个素材还没处理 |
| 晚上健身完 | 进健身模块 | 三十秒内打完卡，看到某动作的重量变化 |
| 吃完一顿饭 | 进饮食模块 | 点几下记完，看到今天还剩多少热量额度 |
| 跟客户聊完 | 进咨询模块 | 立刻记下要点和"下一步做什么" |
| 周末复盘 | 进数据与设置 / 各模块 | 看到本周各摊子的活动量，导出一份备份落到别处 |

### 1.3 硬性约束

- 只在本机浏览器里本地运行，不部署、不联网、不需要登录。
- 刷新、关闭浏览器、重新启动电脑后，数据不能丢。
- 不做手机端，只保证桌面浏览器可用。
- 页面不依赖任何外部网络资源（不引 CDN、不加载在线字体和图标库）。

---

## 2. 整体页面框架和导航结构

### 2.1 启动方式

桌面放一个可双击的启动脚本，双击后：

1. 在本机启动一个只监听 `127.0.0.1` 的轻量服务；
2. 自动用系统默认浏览器打开页面；
3. 关掉终端窗口即停止服务，数据文件不受影响。

> 除启动的这一刻外，使用过程中不需要接触命令行。

### 2.2 导航分区

侧边栏固定九个入口，按用途分成四组，不按字母或重要性平铺：

| 分区 | 模块 | 定位 |
|---|---|---|
| 首页与计划 | 首页总览 | 默认落地页，只看不改细节 |
| | 今日计划 | 唯一按"天"组织的主线 |
| 工作 | 自媒体 | 选题到发布的全流程 |
| | 开发工作 | 项目、里程碑、功能、Bug、日志、计时 |
| | 咨询工作 | 客户、沟通、跟进 |
| 生活 | 健身计划 | 计划模板与打卡 |
| | 饮食计划 | 三餐、热量、体重 |
| | 游戏娱乐 | 在玩、待玩、时长 |
| 设置 | 数据与设置 | 备份、恢复、偏好 |

### 2.3 页面通用骨架

- **左侧固定侧边栏** + **右侧内容区**，内容区随导航切换，页面不整页刷新。
- 地址栏带路由（如 `#/today`、`#/media`）。**刷新页面必须停在当前模块**，不跳回首页。
- 内容区右上角一个不打扰的存盘提示：改完显示「已保存 12:31」，两秒后自动淡出。
- 全站只有一个新增范式：**就地输入 + 回车**，不使用弹窗表单。
- 删除一律「点一次变红、再点一次才真删」，不使用确认弹窗。
- 每个模块在无数据时，显示一句引导语 + 一个主按钮，不留空白页。

### 2.4 全局快捷键

| 快捷键 | 行为 |
|---|---|
| `Cmd + K` | 呼出「快速记一笔」浮层。输入内容后选择存为：快速备忘 / 今日任务（可顺手选归属和优先级）/ 某个模块的条目 |
| `Enter` | 提交当前输入 |
| `Esc` | 取消当前输入 / 关闭浮层 |

---

## 3. 首页的内容结构

首页**不重复模块内部内容**，只给"一眼看现状"，且允许就地动手。自上而下四块：

### 3.1 顶部条

- 左侧：今天的日期和星期（如「9 月 15 日 · 周二」）。
- 右侧：**今日重点**，一句可点可编辑的话。写在这里的内容同步存进当天数据。

### 3.2 今日计划卡（左，占主宽度）

- 右上角显示「2 / 7 已完成」，下方一条细进度条。
- 列出当天未完成的任务（首页最多显示 5 条，超出显示「还有 N 条」），每条带归属标签。
- **可以直接在首页勾选完成**，不用跳进今日计划页。
- 卡片底部一个输入框：「加一条，回车保存」，旁边提示可设置 归属 / 时间 / 优先级。
- 点击卡片标题进入今日计划页。

### 3.3 快速备忘卡（右）

- 一个输入框（提示「随手记一句，回车存下」），回车即存。
- 下方列出最近 3 条备忘，按时间倒序，每条显示日期。
- 每条备忘可「转成今日任务」，也可删除。

### 3.4 模块摘要卡（下方 3 列 × 2 行）

每张卡显示一个核心数字 + 一行补充说明，**点击整卡进入对应模块**。

| 卡 | 主数字 | 补充说明 | 数据来源 |
|---|---|---|---|
| 自媒体 | 本周已发布 N 条 | 待处理素材 N 个 | 本周（周一至今天）发布的内容数；素材中状态非"已完成"的数量 |
| 开发工作 | 进行中 N 项 | 有 N 个致命 Bug 没修 / 待修 Bug N 个 / 今日已计时 X 小时 Y 分（按这个优先级取第一条） | 所有项目下状态为"进行中"的功能数；致命 Bug 优先顶上来，其次待修总数，都没有才显示计时 |
| 咨询工作 | 今日待跟进 N 位 | 有 N 项已逾期（逾期时标红） | 到期日 ≤ 今天的未完成待跟进事项 |
| 健身计划 | 今天练什么（取模板主题名） | 本周已练 N 次 | 今天在计划模板里配置的主题；本周打卡记录条数 |
| 饮食计划 | 今日已记 X 千卡 | 目标 Y · 还差 N 餐没记 | 今日四餐热量之和；目标值来自设置 |
| 游戏娱乐 | 本周 X 小时 | 在玩 N 款 · 待玩 N 款 | 本周时长记录之和；在玩 / 待玩清单条数 |

---

## 4. 每个模块的用途

| 模块 | 一句话用途 | 明确不做什么 |
|---|---|---|
| 首页总览 | 十秒看清今天和全局状态 | 不做模块内部的详细编辑 |
| 今日计划 | 管住"今天"这一条线，包括没做完的事 | 不做跨越多天的项目计划、不做甘特图 |
| 自媒体 | 从选题到发布，管住内容流程和待办素材 | 不自动抓取任何平台数据，播放点赞手工填 |
| 开发工作 | 管住几个项目各自的进度和投入时间 | 不接 Git，不扫描代码目录，不跑命令 |
| 咨询工作 | 管住客户、每次沟通说了什么、下一步做什么 | 不做合同金额、发票、财务记账 |
| 健身计划 | 今天该练什么，练了多重，有没有在进步 | 不内置动作库和训练课程，动作由自己定义 |
| 饮食计划 | 吃了什么、还剩多少额度、体重怎么走 | 不联网查食物热量，热量由自己的食物库提供 |
| 游戏娱乐 | 在玩什么、玩多久，和别的摊子对比看得见 | 不接平台账号，不统计游戏成就 |
| 数据与设置 | 数据在自己手里：备份、恢复、清空、偏好 | 不做账号、权限、分享 |

---

## 5. 每个模块第一版最必要的数据和操作

> 原则：字段只保留第一版真正会用到的；宁可少一个字段，不要多一个永远空着的输入框。

### 5.1 首页总览

- **数据**：不新增独立数据，全部来自其他模块的汇总计算。
- **操作**：勾选首页上的今日任务、编辑今日重点、写快速备忘、备忘转任务、点摘要卡跳转。
- **快速备忘数据**：`id`、`正文`、`创建时间`。

### 5.2 今日计划

- **数据**
  - 每日记录：`日期`、`今日重点`（一句话）、`任务列表`
  - 任务：`id`、`标题`、`归属`（首页/自媒体/开发/咨询/健身/饮食/游戏/无）、`时间段`（可选，如 09:30–10:30 或单点）、`优先级`（高/中/无）、`是否完成`、`完成时间`、`排序`
- **操作**
  - 新增任务（回车即存，默认无归属）、编辑标题、删除（二次确认）
  - 勾选完成 / 取消完成；已完成项沉到列表底部，可折叠
  - 设置/修改 归属、时间段、优先级
  - 拖动排序
  - 切换日期：前一天 / 后一天 / 回到今天
  - 编辑当天「今日重点」
  - **次日顺延**：第二天打开时，若前一天有未完成任务，页面顶部出现提示条「昨天还有 N 项没做完」，提供两个选项：
    - **顺延到今天** → 未完成项复制到今天，昨天记录保持原样（昨天仍是"没做完"的历史事实）
    - **留在昨天** → 今天列表不变，提示条消失
  - 不自动顺延、不弹窗拦截，由使用者决定

### 5.3 自媒体

- **数据**
  - 选题：`id`、`标题`、`阶段`（**灵感捕获 / 脚本/制作 / 待发布 / 已发布**）、`平台`、`创建日期`、`备注`
  - 内容：`id`、`标题`、`平台`、`发布日期`、`链接`、`播放数`、`点赞数`、`完播率`、`互动率`、`关联选题 id`
  - 素材：`id`、`名称`、`类型`（视频/音频/图片/字幕）、`状态`（待处理/处理中/已完成）、`关联内容 id`、`备注`
- **创作工作流**：**四阶段自由拖拽看板** —— `灵感捕获` ➔ `脚本/制作` ➔ `待发布` ➔ `已发布`，
  **卡片拖动即改阶段**；每张卡另有「→ 下一阶段」按钮作为不依赖拖动的备用路径。
  每一列空着时给各自的引导语。
  旧数据迁移：第一版是「灵感 / 制作中 / 已发布」三阶段，读到旧数据时自动映射（`灵感` → `灵感捕获`、`制作中` → `脚本/制作`），幂等，认不出的阶段名兜到第一列，不丢选题。
- **操作**
  - 卡片拖到"已发布"时，就地展开一小行表单补 平台 / 日期 / 链接（不是弹窗）
  - 看板**右侧固定一块「内容记录」**：可以**直接登记一条已发布的内容**（标题 / 平台 / 发布日 / 链接），不必先建选题；下面列出最近 8 条，就地填 播放数 / 点赞数、可删除
  - 卡片进入"已发布"之后，卡片上直接显示 已发日期 · 播放 · 赞，不用点进去看
  - 内容可随时回来补 播放数 / 点赞数
  - **数据复盘面板**：手绘 SVG 折线/柱状图看 播放量 / 点赞数 / 完播率 / 互动率 随时间的变化（零依赖，不引任何图表库）
  - 素材新增、改状态、删除；可关联到某条内容
  - 视图切换：选题池（创作流程）/ 内容日历（月历，有内容的日期打点，点开看当天）/ 素材与待办 / 数据回看（本周发布数、各平台条数分布、趋势图）
  - 平台选项：YouTube / B站 / 抖音 / 小红书 / 其他（可在设置里增删）

### 5.4 开发工作

**层级架构（自上而下严格五层）**：项目 ➔ 里程碑 ➔ 功能列表 ➔ Bug 追踪 ➔ 开发日志。
每层用「所属xx」指回上层，数据是平的不嵌套，删上层时才能精确控制要不要连带删下层。

- **数据**
  - 第一层 项目：`id`、`名称`、`状态`（进行中/暂停/已完成）、`仓库路径或链接`（纯文本）、`备注`
  - 第二层 里程碑：`id`、`所属项目`、`名称`、`状态`（未开始/进行中/已完成）、`目标日期`、`备注`
  - 第三层 功能：`id`、`所属项目`、`所属里程碑`、`标题`、`状态`（待办/进行中/已完成）、`优先级`（高/中/低/无）、`创建日期`、`完成日期`、`归档`
  - 第四层 Bug：`id`、`所属项目`、`所属里程碑`、`所属功能`、`标题`、`严重程度`（致命/严重/一般/轻微）、`状态`（待修/修复中/已修复/不修）、`创建日期`、`完成日期`、`归档`
  - 第五层 开发日志：`id`、`所属项目`、`类别`、`关联id`、`标题`、`动作`、`说明`、`时间`
  - 辅助：笔记（`id`、`所属项目`、`正文`、`创建时间`）、计时记录（`id`、`所属项目`、`开始时间`、`结束时间`、`时长`）
- **操作**
  - 新建项目、改项目状态、删除项目（连带其里程碑/功能/Bug/日志/笔记/计时，二次确认并先报数）
  - 左侧项目列表 + 右侧项目详情
  - 里程碑可增删、设目标日期与状态；**进度由它下面的功能与 Bug 自动推出来**（已完成的百分比），不手工同步
  - 删里程碑**不删**下属的功能与 Bug，只把它们从分组里拿出来
  - 功能三栏看板（待办 / 进行中 / 已完成），卡片可拖动改状态，也可点「→ 下一阶段」「← 退回」
  - **两种视图一键切换**（切模式不丢当前选中项目）：
    - **看板模式（按状态流转）**：待办 / 进行中 / 已完成 三列，卡片可拖动改状态
    - **列表模式（按优先级与里程碑聚合）**：再选「按里程碑聚合」或「按优先级聚合」；
      按里程碑时没归入的落到「未归入里程碑」，空里程碑也保留（"还没开始"本身是信息）；
      按优先级时空的档位收掉；组内先按优先级（高→中→低→无）、再按状态（进行中→待办→已完成）排序
  - **完成勾选联动**：功能与 Bug 卡片/行左侧都有一个小方框，勾上即「完成」——
    功能变「已完成」、Bug 变「已修复」，两者都记完成日期、打上归档标记，
    并**自动在开发日志里落一条**（`完成功能「xx」` / `修复 Bug「xx」`）。
    取消勾选则状态退回「待办 / 待修」、撤销归档，并**撤掉那条自动生成的日志**。
    （为什么是撤掉而不是留一条"已撤销"：开发日志是当"发生过什么"看的，误勾一次留一条假记录会把日志污染。手工写进去的日志带 `来源: 手工`，不受这条链路影响。）
    重复勾同一个是幂等的，不会产生第二条日志。
  - **归档**：已完成的功能与已收尾的 Bug 收进归档区，看板上默认收起（只显示条数与「展开归档」按钮），避免看板越用越长
  - Bug 追踪：按严重程度排序，状态下拉（待修 / 修复中 / 已修复 / 不修），`已修复` 与 `不修` 都算收尾
  - 项目的功能或 Bug 加进今日计划（标题带项目名，归属 dev）
  - 项目卡上「开始计时 / 停止计时」，同一时间只允许一个项目在计时
  - 项目详情显示「今日累计 X 小时 Y 分」和「累计 Z 小时」
  - 写片段笔记、删笔记（笔记是片段级，不建文件树）
- **旧数据迁移**：第一版把任务挂在 `项目[].任务列表`，现在统一并进【功能列表】（`所属项目` 指回来，`所属里程碑` 为空）。读盘与导入备份时自动迁移，幂等，跑两次不会重复。

### 5.5 咨询工作

- **数据**
  - 客户：`id`、`名称`、`对接人`、`联系方式`（纯文本）、`合作状态`（洽谈中/合作中/已结束）、`开始日期`、`备注`
  - 沟通记录：`id`、`所属客户`、`日期`、`要点`、`下一步`（必填）
  - 待跟进：`id`、`所属客户`、`事项`、`到期日`、`是否完成`
  - 交付物：`id`、`所属客户`、`名称`、`交付日期`、`状态`（未交/已交）
  - 工时：`id`、`所属客户`、`日期`、`时长`
- **操作**
  - 新增客户、改合作状态、删除客户（连带记录，二次确认）
  - 左侧客户列表 + 右侧客户详情
  - 记一次沟通：日期、要点、**下一步（必填）**，按时间倒序排成一条线
  - 加待跟进、标完成；**到期日 ≤ 今天且未完成的标红**
  - 交付物勾"已交"并记交付日期
  - 记工时（按次累加）
  - 顶部汇总：本周跟进次数、待跟进总数、逾期数、本月工时

### 5.6 健身计划

- **数据**
  - 计划模板：`星期`（一~日）→ `主题`（如「腿部训练」）+ 动作列表（`动作名`、`目标组数`、`目标次数`）
  - 打卡记录：`id`、`日期`、`主题`、动作明细（`动作`、`组数`、`次数`、`重量`）、`备注`
  - 动作名列表：从打卡与模板中自动累积，用于匹配和趋势
- **操作**
  - 编辑星期模板（每天可设主题 + 若干动作）
  - 「开始今天的训练」：自动带出今天模板里的动作，逐个填 重量 / 组数 / 次数
  - 当天休息日也能手动新建一次训练
  - 查看历史记录（按日期倒序）
  - 查看某个动作的重量变化（简单柱状，按日期）
  - 本周已练次数统计

### 5.7 饮食计划

- **数据**
  - 食物库：`id`、`名称`、`单位`（份/克/个）、`热量`、`蛋白质`（可选，填了才算）
  - 每日记录：`日期` → 四餐（早/午/晚/加餐）各自的条目（`食物名`、`数量`、`热量`）
  - 饮水：`日期` → 已喝杯数
  - 体重：`日期` → `体重`
  - 每日热量目标（存在设置里）
- **操作**
  - 记一餐：输入菜名时**自动匹配食物库**，选中即带出热量，数量可改；库里没有的可以填了直接入库
  - 食物库维护（增删改名字、单位、热量、蛋白质）
  - 饮水用一排格子点击加减
  - 记体重，看体重变化曲线
  - 今日汇总条：已记热量 / 目标、蛋白质合计、饮水杯数

### 5.8 游戏娱乐

- **数据**
  - 在玩：`id`、`名称`、`平台`、`进度`（0–100）、`开始日期`
  - 待玩：`id`、`名称`、`平台`、`加单日期`
  - 时长记录：`id`、`游戏 id`、`日期`、`时长`
- **操作**
  - 新增游戏（可放入在玩或待玩）
  - 在玩的游戏拖动进度条；进度到 100 可一键移到"已通关"
  - 待玩清单**直接拖进"在玩"**
  - 「开始玩」计时 / 手动补一段时长
  - 顶部汇总：本周娱乐时长；每个游戏显示累计时长

### 5.9 数据与设置

- **数据**：`昵称`、`每日热量目标`、`每周训练目标次数`、`每周发布目标条数`、`平台选项列表`、`模块显隐与排序`
- **操作**
  - **导出备份**：下载一个带日期的 JSON（如 `工作台备份-2026-09-15.json`）
  - **导入恢复**：选一个备份文件恢复；**导入前先把当前数据自动存成一份"导入前备份"**，防手滑
  - **打开数据文件夹**：在系统里直接定位到数据文件所在目录（由本地服务执行，只允许打开这一个固定目录）
  - **按模块清空**：只清某一个模块的数据（二次确认，并提示会先自动备份）
  - 修改偏好项：昵称、各项目标值、平台选项、模块显隐与排序

---

## 6. 首页、今日计划和各模块之间的关系

### 6.1 关系图

```
                    ┌──────────────┐
                    │   设置 / 目标值  │
                    └───────┬──────┘
                            │ 提供目标（热量、训练次数、发布条数）
                            ▼
  ┌──────────────────────────────────────────────────┐
  │                     首页总览                       │
  │  只读汇总：六张摘要卡 + 今日计划卡 + 快速备忘卡      │
  └───┬──────────┬──────────┬──────────┬─────────────┘
      │ 点卡跳转   │ 勾任务   │ 写备忘   │ 备忘转任务
      ▼          ▼          ▼          ▼
  ┌────────┐ ┌─────────────────┐ ┌──────────────┐
  │ 六个模块 │ │    今日计划      │ │   快速备忘     │
  └───┬────┘ │  归属标签 → 指向模块 │ └──────┬───────┘
      │      └────────┬────────┘        │
      │               │ 「加进今日计划」      │
      └───────────────┘◄────────────────┘
```

### 6.2 双向联动的两条线

**线一：首页 → 各模块（只读汇总 + 快捷操作）**

- 六张摘要卡的数字全部由模块数据实时算出，首页不存自己的数据副本，因此不会出现"首页数字和模块里对不上"。
- 在首页勾选任务、写备忘、备忘转任务，等同在今日计划页面操作，两处状态一致。

**线二：今日计划 ↔ 各模块（归属标签）**

- 今日计划里的每条任务可打一个**归属标签**（自媒体 / 开发 / 咨询 / 健身 / 饮食 / 游戏 / 无）。
- 在模块内部也能反向把条目"加进今日计划"：例如自媒体选一条选题、咨询选一个客户待跟进、健身的今日训练，都可以一键生成一条带好归属标签的今日任务。
- 归属只是标签，不改变数据的归属关系：任务只存在今日计划里，模块数据只存在模块里，两边各自独立、靠标签关联。这样任何一边坏了都不会牵连另一边。

### 6.3 数据流向的一条例

在饮食模块点午餐加一个鸡蛋 → 当日热量记录更新 → 饮食模块的今日汇总变化 → 首页饮食摘要卡同步变化。全程不需要手动刷新，也没有"保存"按钮。

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 存放位置

- 单个 JSON 文件，放在项目目录下：`工作台/data/data.json`。
- 文件用 **UTF-8 中文键名**，双击可读，能直接看懂里面存了什么。
- 「数据与设置」页面显著位置显示这个文件的完整路径。

### 7.2 文件结构（第三版）

```json
{
  "版本": 3,
  "设置": { "昵称": "", "热量目标": 1800, "每周训练目标": 3, "每周发布目标": 2, "平台选项": [], "隐藏模块": [] },
  "每日": { "2026-09-15": { "今日重点": "", "任务": [] } },
  "备忘": [],
  "自媒体": { "选题": [], "内容": [], "素材": [] },
  "开发": { "项目": [], "里程碑": [], "功能": [], "Bug": [], "日志": [], "笔记": [], "计时": [] },
  "咨询": { "客户": [], "沟通": [], "待跟进": [], "交付物": [], "工时": [] },
  "健身": { "计划模板": {}, "打卡": [] },
  "饮食": { "食物库": [], "记录": {}, "饮水": {}, "体重": [] },
  "游戏": { "在玩": [], "待玩": [], "时长": [] }
}
```

实际实现时的几处结构细节（都属于「怎么放进文件」的问题，不影响功能）：

- **开发改成五个平层数组**（`项目` / `里程碑` / `功能` / `Bug` / `日志`），靠「所属项目 / 所属里程碑 / 所属功能」指回上层，不互相嵌套。第一版把任务藏在 `项目[].任务列表` 里，第二版并进了 `功能`，读盘与导入时自动迁移（见 §5.4）。
- **游戏的「已通关」用「在玩」里每条记录的状态字段表达**（`在玩[].状态 = 在玩 | 已通关`），没有单独开一个数组。
- **选题的阶段名第二版改过**：三阶段 → 四阶段，旧名在读盘与导入时自动映射（见 §5.3）。

### 7.3 写入要求

1. **改动即写盘**，不设"保存"按钮；写盘完成后才在界面上提示「已保存」。
2. **原子写入**：先写 `data.json.tmp`，写成功后再替换 `data.json`，避免写一半断电导致文件损坏。
3. **自动留一版上一份**：每次成功替换前，把替换前的 `data.json` 存为 `data.json.bak`，只保留最近一份。
4. 文件被外部改坏或解析失败时：不静默丢弃，页面顶部给出明确提示，并提供「从 data.json.bak 恢复」和「用空白数据继续」两个选项。

### 7.4 备份与恢复要求

| 能力 | 要求 |
|---|---|
| 导出备份 | 一键下载 `工作台备份-YYYY-MM-DD.json`，内容与当前数据文件完全一致 |
| 导入恢复 | 选择备份文件后恢复；**恢复前自动把当前数据另存为 `导入前备份-时间戳.json`**，并告知存放路径 |
| 版本兼容 | 备份文件带 `版本` 字段。版本号高于当前程序时，拒绝导入并说明原因，不尝试猜测解析 |
| 手动备份 | 数据目录可被任意复制（拷 U 盘、丢网盘均可），文件本身自包含，不依赖任何缓存或系统状态 |
| 迁移 | 换电脑时把 `data` 目录整体拷过去即可继续使用 |

### 7.5 边界说明

- 唯一会丢数据的操作是**使用者自己删除数据文件**，这是刻意保留的行为（否则文件藏得太深，想找都找不到）。
- 不做加密。数据是明文，能看懂是这个设计的优点，也是它需要放在自己电脑上的原因。

---

## 8. 第一版必须完成的功能

1. 本地服务 + 可双击的启动脚本，双击后自动打开浏览器，除启动外无需接触命令行。
2. 数据文件读写打通：载入、原子写盘、自动留上一版、损坏时的提示与恢复入口。
3. 侧边栏九个导航项 + hash 路由，刷新停在当前模块。
4. 首页总览：日期、今日重点、今日计划卡（可直接勾选和新增）、快速备忘卡、六张模块摘要卡。
5. 今日计划：任务增删改、归属/时间/优先级、拖动排序、日期前后切换、**次日顺延提示条**。
6. 快速备忘：新增、删除、转成今日任务。
7. 自媒体：三列看板选题池（拖动改阶段）、发布时补平台/日期/链接、内容数据回填、素材与待办清单、内容日历。
8. 开发工作：五层结构（项目 ➔ 里程碑 ➔ 功能列表 ➔ Bug 追踪 ➔ 开发日志）、功能三栏拖动、里程碑进度自动汇总、项目计时、今日/累计时长、片段笔记。
9. 咨询工作：客户档案、沟通时间线（下一步必填）、待跟进（到期标红）、交付物、工时、顶部汇总。
10. 健身计划：星期模板、开始今日训练并打卡、历史记录、单动作重量趋势、本周次数。
11. 饮食计划：食物库、四餐记录（输入自动匹配食物库）、饮水格子、体重记录、今日汇总。
12. 游戏娱乐：在玩（可改进度）、待玩（可拖进在玩）、时长记录与本周汇总。
13. 数据与设置：导出、导入（含导入前自动备份）、打开数据文件夹、按模块清空、偏好项。
14. 全局 `Cmd + K` 快速记一笔。
15. 各模块的空状态引导。
16. 全站统一的交互细则：就地输入回车即存、删除需二次点击、改动即时存盘并显示「已保存」。

---

## 9. 第一版暂时不做的功能

| 不做 | 说明 |
|---|---|
| 登录、账号、多用户 | 单人本机使用，不需要 |
| 云同步、多设备 | 换电脑靠拷文件解决 |
| 手机端适配与响应式优化 | 只保证桌面浏览器 |
| 任何联网行为 | 不调外部 API、不抓平台数据、不加载 CDN、不加载在线字体与图标 |
| 推送通知、桌面提醒、邮件 | 到期事项只在界面上标红 |
| Git / IDE / 系统级集成 | 只在"打开数据文件夹"这一处调用本机命令 |
| 全局搜索 | 模块不多，侧边栏直达 |
| 图表库与动画特效 | 趋势用轻量自绘，不引第三方图表库 |
| 数据加密 | 明文存放，便于自己查看 |
| 报表导出（PDF / Excel） | 需要时用备份 JSON 另行处理 |
| 自动抓取平台数据 | 播放量、点赞数一律手工填 |
| 内置动作库、食物热量库 | 由使用者自己维护 |

---

## 10. 验收标准

> 每条都可实际操作检查，逐条勾选。全部通过才算第一版完成。

### 10.1 持久化与本地化

- [ ] 在任意模块改数据（如加一条任务），**刷新页面**后数据仍在且停在当前模块，不回首页。
- [ ] **关闭浏览器再重新打开**，数据仍在。
- [ ] **重启电脑后**重新启动，数据仍在。
- [ ] **清理浏览器缓存 / 换另一个浏览器**打开，数据不受影响（数据不在浏览器里）。
- [ ] **断开网络**后启动并使用，所有功能正常，页面不出现任何网络错误提示。
- [ ] 浏览器控制台无对外域名的请求记录。

### 10.2 启动与运行

- [ ] 双击启动脚本，浏览器自动打开，无需手动输入地址。
- [ ] 除启动外，整个使用流程中不需要输入任何命令。
- [ ] 关闭终端窗口后服务停止；重新双击可再次启动，数据不变。

### 10.3 首页与今日计划

- [ ] 在今日计划新增一条任务，首页今日计划卡**立即**出现该条。
- [ ] 在首页勾选一条任务完成，今日计划页的完成数和进度条同步变化。
- [ ] 首页六张摘要卡的数字与对应模块内的统计结果一致。
- [ ] 在任意模块新增一条当日记录（如健身打卡、饮食记录），对应摘要卡数字**立即**更新。
- [ ] 点任意摘要卡，跳到对应模块。
- [ ] 前一天留下 2 条未完成任务，今天打开今日计划：出现提示条「昨天还有 2 项没做完」。
- [ ] 点「顺延到今天」：两条出现在今天的列表里，昨天的记录仍完整保留。
- [ ] 点「留在昨天」：今天的列表不变，提示条消失，昨天的记录仍完整保留。
- [ ] 未做任何操作时，任务**不会**被自动顺延。

### 10.4 各模块基本可用性

- [ ] 九个导航项都能打开，均无报错、无空白页。
- [ ] 每个模块在无数据时显示引导语和主按钮。
- [ ] 自媒体：拖动选题卡片可改阶段；拖到"已发布"时能补平台/日期/链接；内容日历上有内容的日期打点正确。
- [ ] 开发工作：同一时间只能有一个项目在计时；停止后今日累计时长正确。
- [ ] 咨询工作：新增沟通记录时「下一步」为空则不能提交；到期未完成的待跟进在界面上标红。
- [ ] 健身计划：模板里给星期二配了动作，到星期二打开能自动带出这些动作。
- [ ] 饮食计划：输入"鸡蛋"能匹配到食物库条目并自动带出热量；今日汇总数字等于四餐条目热量之和。
- [ ] 游戏娱乐：待玩条目能拖进"在玩"。
- [ ] 修改任意一处数据后，右上角出现「已保存」提示，且**全程没有点过任何保存按钮**。

### 10.5 交互细则

- [ ] 点击删除一次，只是变红提示，数据未删；再点一次才真正删除。
- [ ] 新建条目全程没有出现弹窗表单。
- [ ] `Cmd + K` 能呼出快速记一笔，并存成指定的目标位置。
- [ ] `Esc` 能取消当前输入。

### 10.6 数据文件、备份与恢复

- [ ] 数据文件路径固定，且出现在「数据与设置」页面的显示中。
- [ ] 用文本编辑器打开数据文件，能看到中文键名，内容与界面一致。
- [ ] 导出备份：得到 `工作台备份-YYYY-MM-DD.json`，内容与当前数据一致。
- [ ] 导入刚才的备份，数据恢复成导出时的样子。
- [ ] 导入前自动生成了一份「导入前备份」文件，路径有明确提示。
- [ ] 故意把数据文件内容改成非法 JSON 后重启：页面给出明确提示，并能从 `data.json.bak` 恢复成功。
- [ ] 从「数据与设置」能一键在系统里定位到数据文件夹。
- [ ] 按模块清空只会清掉该模块，其他模块数据完好。

---

## 附：关键设计取舍（记录在案，避免以后反复讨论）

| 取舍 | 选择 | 代价 |
|---|---|---|
| 运行方式 | 本地小服务 + 双击启动脚本 | 每次使用多一个"启动"动作，换来数据落在真实文件里、可备份可迁移 |
| 数据格式 | 单个 JSON，中文键名 | 用脚本处理要写中文键；换来随时能打开看懂 |
| 顺延策略 | 提示 + 人工选择，不自动顺延 | 每天多一次点击，换来"昨天到底做没做完"这个事实不被篡改 |
| 首页数字 | 实时计算，不存副本 | 每次打开要算一遍（数据量小，无感），换来永远不出现前后不一致 |
| 归属标签 | 只做标签，不做数据搬迁 | 任务仍只存在于今日计划，换来模块之间互不牵连 |
| 模块配色 | 五色区分模块 | 视觉上更花，换来扫一眼就能分辨条目属于哪块 |
