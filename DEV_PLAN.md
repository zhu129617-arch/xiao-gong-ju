# 开发计划 · 个人工作生活工作台（第一版）

> 依据：`/Users/zhu/WorkBuddy/小工具/PRD.md` v1.0
> 日期：2026-09-15　｜　文档版本：v1.0
> 说明：本文件是可直接照着执行的施工单。每一批都有「交付物」和「自检」，**每批做完停下来让使用者实际打开看，确认后再进下一批**。

---

## 0. 技术选型（先定死，避免边做边改）

| 项 | 选择 | 理由 |
|---|---|---|
| 后端 | Node 内置 `http` / `fs` / `path` / `child_process` | 零 npm 依赖。没有 `node_modules`，换电脑拷过去就能跑，不会出现依赖装不上 |
| 前端 | 原生 HTML + CSS + ES Module，无框架、无构建 | 满足 PRD「不加载 CDN」；没有构建步骤，改完刷新即生效 |
| 数据 | 单个 JSON 文件，整文件读写 | 数据量小（预计 < 1 MB），整文件写最不容易出错，不需要合并逻辑 |
| 监听地址 | `127.0.0.1:41873`（备用 41874–41876） | 只监听本机回环，局域网内其他设备也访问不到 |
| 进程模型 | 单进程，前台运行 | 关终端窗口即停止；不做后台常驻、不做开机自启 |

### 已实测的环境事实

- `/usr/local/bin/node` → v24.20.0（系统安装）
- `~/.workbuddy/binaries/node/versions/22.22.2/bin/node` → v22.22.2（WorkBuddy 自带）
- 端口 41873 / 41874 / 41875 / 41876 均未被占用

启动脚本必须**依次探测**这两个位置，任一个能用即可（代码只用 ES2020 以内语法，两个版本都跑得动）。

---

## 1. 目录结构

```
/Users/zhu/WorkBuddy/小工具/
├── PRD.md                        # 已存在，不动
├── DEV_PLAN.md                   # 本文件
└── 工作台/                        # 第一版新建，应用全部内容在这里
    ├── 启动工作台.command          # 双击启动（UTF-8，无 BOM）
    ├── server/
    │   └── server.js             # 静态服务 + 4 个 API + 原子写盘
    ├── public/
    │   ├── index.html
    │   ├── css/
    │   │   ├── base.css          # 变量、排版、侧边栏、布局
    │   │   └── components.css    # 卡片、标签、输入框、按钮、空状态
    │   ├── icons.svg             # 图标精灵（本地自带，不引字体库）
    │   └── js/
    │       ├── app.js            # 入口：路由 + 挂载
    │       ├── store.js          # 数据读写、防抖存盘、订阅
    │       ├── summary.js        # 所有汇总计算（首页与模块共用，唯一的真源）
    │       ├── dates.js          # 日期工具（本地时区，YYYY-MM-DD）
    │       ├── ui.js             # 通用控件
    │       └── views/
    │           ├── home.js       ├── today.js      ├── media.js
    │           ├── dev.js        ├── consult.js    ├── fitness.js
    │           ├── diet.js       ├── games.js      └── settings.js
    └── data/                     # 首次启动自动创建
        ├── data.json             # 主数据文件
        ├── data.json.bak         # 自动保留的上一版
        └── 导入前备份-*.json       # 导入时自动生成
```

---

## 2. 接口契约（服务端全部实现，前端只调这 4 个）

| 方法 | 路径 | 行为 |
|---|---|---|
| `GET` | `/api/data` | 读取并返回 `data/data.json`。文件不存在 → 用默认结构创建后返回；解析失败 → 返回 `500` 和错误类型（前端据此弹恢复引导） |
| `PUT` | `/api/data` | 覆盖写入。**必须**：写 `data.json.tmp` → fsync → 把原 `data.json` 改名存为 `data.json.bak` → `rename` 临时文件为主文件。任一步失败则保持原文件不动并返回错误 |
| `POST` | `/api/open-folder` | 在系统里打开数据目录。**不接受任何来自请求的路径参数**，服务端写死 `data` 目录的绝对路径，避免被当作任意命令执行入口 |
| `GET` | `/` 与 `/public/*` | 静态文件。响应头带 `Cache-Control: no-cache`，避免改了代码浏览器还用旧缓存 |

约定：
- 只有 127.0.0.1 能访问，不做任何鉴权（单人本机使用，加鉴权只是摆设）。
- `PUT` 请求体上限设为 20 MB，超出直接拒绝。
- 所有错误响应统一为 `{ "错误": "描述" }`，前端能直接显示成人话。

---

## 3. 前端数据层设计（这是全项目最容易做错的地方）

### 3.1 三个必须遵守的规则

1. **内存中只保留一份数据对象。** 所有模块读写同一个对象，任何模块都不允许持有自己的副本。
2. **所有汇总数字一律现算。** 首页六张摘要卡、各模块页头的统计，全部调用 `summary.js` 里的函数，**不允许任何模块提前把数字存进数据文件**。这是 PRD §6 里「首页数字与模块不一致」这个问题的根本解法。
3. **改动即存盘，无保存按钮。** 流程固定为：改内存 → 通知订阅者重渲染 → 300 ms 防抖 → `PUT /api/data` → 成功后右上角显示「已保存 HH:mm」，2 秒淡出。

### 3.2 并发写入的处理

连续快速改动（如连勾五个任务）只发一次请求。若上一次请求还在路上又来了新改动，**不并发发送**，而是排在队列里，等上一次返回后立刻发最新的一份（中间态直接丢弃，因为写的是整份数据）。

### 3.3 store.js 对外接口

```
store.load()                 首次载入（页面启动时调一次）
store.get()                  取整个数据对象（只读用）
store.update(fn)             改数据：传入修改函数，内部改完自动触发存盘
store.onError(fn)            存盘失败时的回调（用于显示错误条）
store.subscribe(fn)          数据变化后重渲染
```

### 3.4 通用控件清单（ui.js，做一次全站复用）

| 控件 | 行为 | 对应 PRD 条款 |
|---|---|---|
| `inlineInput` | 就地输入框，回车提交，Esc 取消 | §2.3 |
| `confirmDelete` | 第一次点击变红并显示「再点一次删除」，3 秒无操作自动复位 | §2.3 |
| `emptyState` | 空状态：一句引导语 + 一个主按钮 | §2.3 |
| `tagChip` | 模块色标签（自媒体蓝 / 开发紫 / 咨询琥珀 / 健身绿 / 饮食粉 / 游戏灰） | 附录 |
| `metricCard` | 摘要卡：标签 + 主数字 + 补充说明，整卡可点 | §3.4 |
| `dragList` | 拖动排序 / 拖动改状态，用原生 `draggable`，不引第三方库 | §5.3、§5.4 |
| `quickCapture` | `Cmd+K` 浮层，选存成备忘 / 今日任务 / 模块条目 | §2.4 |
| `saveBadge` | 右上角「已保存」提示 | §2.3 |

---

## 4. 施工批次

> 共 7 批。**批次 0–2 完成后就有一个能真正用起来的 App**（首页 + 今日计划），后面的模块是往里填。

### 批次 0 · 能跑起来的空壳

**交付物**：双击能打开一个页面，数据文件自动生成。

**任务清单**
- [ ] 建 `工作台/` 及 `server/`、`public/`、`data/` 目录
- [ ] 写 `server/server.js`：静态服务 + `GET /api/data` + `PUT /api/data`（原子写）+ 端口探测
- [ ] 写默认数据结构（PRD §7.2 的全量字段，空数组/空对象）
- [ ] 写 `public/index.html`：先只显示「工作台已启动」
- [ ] 写 `启动工作台.command`：探测 node → 起服务 → 等服务就绪 → `open http://127.0.0.1:41873` → 前台保留日志
- [ ] 给 `.command` 加可执行权限

**自检**
1. 双击脚本，浏览器自动打开并显示「工作台已启动」
2. `data/data.json` 自动生成，用文本编辑器打开能看到完整默认结构、键名是中文
3. 关掉终端窗口，服务停止；再次双击能重新启动
4. **再次双击时不会覆盖已有数据**（手动往 data.json 里加一条测试记录，重启后仍在）

---

### 批次 1 · 外壳与数据层

**交付物**：九项导航可切换、刷新停在原页、数据能读能写、通用控件可用。

**任务清单**
- [ ] `base.css`：配色变量（含六个模块色）、排版（正文 13px / 标题 15px·14px·13px、只用 400 与 500 两个字重）、侧边栏 + 内容区布局
- [ ] `components.css`：卡片、标签、输入框、按钮、空状态
- [ ] `app.js`：hash 路由（`#/home`、`#/today`、`#/media`、`#/dev`、`#/consult`、`#/fitness`、`#/diet`、`#/games`、`#/settings`），九个空白 view 骨架
- [ ] `store.js`：载入、防抖存盘、写入队列、错误回调、订阅
- [ ] `saveBadge`：右上角「已保存 HH:mm」
- [ ] `dates.js`：`todayKey()`、`shiftDay()`、`formatDisplay()`，全部用本地时间，不做 UTC 转换
- [ ] 动态生成 `index.html` 的函数（用 `document.title` 与 hash 定位，不要手写九个 HTML 文件）
- [ ] 图标：手写少量内联 SVG 存进 `icons.svg`，**不引入任何图标字体或 CDN**

**自检**
1. 九项导航都能点开，地址栏 hash 跟着变，无报错
2. 在 `#/fitness` 按 F5 刷新，仍停在健身页，不跳回首页
3. 随便写一个临时按钮调 `store.update()`，右上角出现「已保存」，`data.json` 内容同步变化
4. 断网后刷新，页面正常、无任何网络错误
5. 九个 view 都显示空状态引导（暂时可以先共用一句占位文案）

---

### 批次 2 · 今日计划 + 首页（核心闭环）

**交付物**：App 真正可用的最小版本。

**任务清单**
- [ ] `summary.js` 第一批函数：
  - `todayTaskStats(数据)` → 完成数 / 总数
  - `mediaSummary()` `devSummary()` `consultSummary()` `fitnessSummary()` `dietSummary()` `gamesSummary()`
- [ ] `today.js`
  - [ ] 任务增删改、勾选完成（完成后沉底并可折叠）
  - [ ] 归属 / 时间段 / 优先级 三个可编辑字段
  - [ ] 拖动排序（`dragList`）
  - [ ] 日期切换：前一天 / 后一天 / 回到今天
  - [ ] 编辑当天「今日重点」
  - [ ] **次日顺延提示条**：前一天有未完成任务时出现，两个按钮
    - 「顺延到今天」→ 未完成项复制到今天，昨天记录保持原样
    - 「留在昨天」→ 今天不变，提示条消失（记住已处理，当天不再出现）
- [ ] `home.js`
  - [ ] 顶部：日期星期 + 可编辑的今日重点
  - [ ] 今日计划卡：进度条、未完成最多 5 条（超出显示「还有 N 条」）、可直接勾选、底部输入框可加任务
  - [ ] 快速备忘卡：输入框回车即存、最近 3 条、可删除、可转成今日任务
  - [ ] 六张摘要卡（数字全部现算，点击跳转对应模块）
- [ ] `quickCapture`：`Cmd+K` 浮层，可存成 备忘 / 今日任务（含归属与优先级）/ 模块条目

**自检**：PRD §10.3 全部条目 + §10.5 全部条目
- 首页加任务 → 今日计划页立即出现；反之亦然
- 首页勾任务 → 进度条与分子变化
- 手工造出「昨天有 2 条未完成」的数据，今天打开出现提示条；两个按钮都试一遍，昨天记录都完整保留
- 不做任何操作时任务**不会**自动顺延

---

### 批次 3 · 三个工作模块

**交付物**：自媒体、开发工作、咨询工作三个页面可用。

**任务清单**
- [ ] `media.js`
  - [ ] 三列看板选题池，卡片拖动改阶段
  - [ ] 拖到「已发布」时就地展开小表单，补 平台 / 日期 / 链接
  - [ ] 内容列表：可回填 播放数 / 点赞数
  - [ ] 素材清单：新增、改状态、删除、关联内容
  - [ ] 内容日历：月历，有内容的日期打点，点开看当天
  - [ ] 数据回看：本周发布数、各平台条数分布
  - [ ] 平台选项从设置里读，默认 YouTube / B站 / 抖音 / 小红书 / 其他
- [ ] `dev.js`
  - [ ] 左侧项目列表 + 右侧项目详情（待办 / 进行中 / 已完成 三栏），任务拖动改状态
  - [ ] 项目计时：开始 / 停止，**同一时间只允许一个项目在计时**
  - [ ] 显示「今日累计」与「累计时长」
  - [ ] 片段笔记增删
- [ ] `consult.js`
  - [ ] 客户列表 + 客户详情（档案 / 沟通时间线 / 待跟进 / 交付物）
  - [ ] 记沟通：要点、**下一步必填**（空则拒绝提交）
  - [ ] 待跟进：到期日 ≤ 今天且未完成 → 标红
  - [ ] 交付物勾「已交」并记日期
  - [ ] 记工时；页头汇总：本周跟进次数、待跟进总数、逾期数、本月工时
- [ ] 三处都补上「加进今日计划」的入口（生成一条带好归属标签的今日任务）

**自检**：PRD §10.4 中自媒体、开发、咨询三条

---

### 批次 4 · 三个生活模块

**交付物**：健身计划、饮食计划、游戏娱乐三个页面可用。

**任务清单**
- [ ] `fitness.js`
  - [ ] 星期模板编辑（一~日，每天可设主题 + 若干动作）
  - [ ] 「开始今天的训练」自动带出当天模板动作，逐个填 重量 / 组数 / 次数
  - [ ] 休息日也能手动新建一次训练
  - [ ] 历史记录（按日期倒序）；单动作重量柱状趋势（轻量自绘 SVG，不引图表库）
  - [ ] 本周已练次数
- [ ] `diet.js`
  - [ ] 食物库增删改（名称、单位、热量、蛋白质可选）
  - [ ] 四餐记录：输入菜名自动匹配食物库，选中带出热量，数量可改；库里没有的可直接入库
  - [ ] 饮水格子点击加减
  - [ ] 体重记录 + 变化曲线
  - [ ] 今日汇总条：已记热量 / 目标、蛋白质合计、饮水杯数
- [ ] `games.js`
  - [ ] 在玩：进度条可拖，到 100 可一键移到「已通关」
  - [ ] 待玩：直接拖进「在玩」
  - [ ] 时长记录：开始玩计时 / 手动补一段
  - [ ] 本周娱乐时长汇总、每款累计时长
- [ ] 三处都补上「加进今日计划」入口

**自检**：PRD §10.4 中健身、饮食、游戏三条

---

### 批次 5 · 数据与设置 + 全局收尾

**交付物**：数据能自己拿在手里。

**任务清单**
- [ ] `settings.js`
  - [ ] 显示数据文件完整路径，一个按钮调 `POST /api/open-folder`
  - [ ] 导出备份：下载 `工作台备份-YYYY-MM-DD.json`（前端由当前内存数据生成，与磁盘内容一致）
  - [ ] 导入恢复：选文件 → **先把当前数据另存为 `导入前备份-时间戳.json`** → 再写入 → 明确提示备份文件路径
  - [ ] 版本校验：备份里的 `版本` 高于当前程序 → 拒绝导入并说明原因
  - [ ] 按模块清空（二次确认 + 自动备份 + 说清会清掉什么）
  - [ ] 偏好项：昵称、热量目标、每周训练目标、每周发布目标、平台选项增删、模块显隐与排序
- [ ] `server.js` 增加：数据解析失败时返回明确错误类型；`POST /api/open-folder`
- [ ] 数据损坏引导条：启动时若 JSON 解析失败 → 顶部提示 + 「从 data.json.bak 恢复」/「用空白数据继续」两个按钮
- [ ] 九个模块的空状态文案全部写成各自的引导语（不能九处一样）
- [ ] 可选的自检脚本 `server/selftest.js`：跑一遍「写入 → 读回 → 制造损坏 → 从 .bak 恢复」，用 `node server/selftest.js` 一条命令验证数据层

**自检**：PRD §10.6 全部 + §10.1 全部 + §10.2 全部

---

### 批次 6 · 整体验收

**交付物**：PRD 第 10 章全部条目勾选完毕。

**任务清单**
- [ ] 逐条走 PRD §10.1 – §10.6，共 6 组约 40 条，逐项打勾，不通过的当场修
- [ ] 键盘操作走一遍：`Enter` 提交、`Esc` 取消、`Cmd+K` 呼出、`Tab` 顺序正常
- [ ] 数据损坏演练：手改坏 `data.json` → 重启 → 走恢复流程
- [ ] 迁移演练：把 `工作台/` 整个目录拷到另一个位置，双击启动，数据完整
- [ ] 检查全站无外部网络请求（浏览器开发者工具 Network 面板过滤，应只有 127.0.0.1）
- [ ] 检查控制台无报错

---

## 5. 批次依赖关系

```
批次 0 ──► 批次 1 ──► 批次 2 ──┬──► 批次 3 ──┐
                              │            ├──► 批次 5 ──► 批次 6
                              └──► 批次 4 ──┘
```

批次 3 和批次 4 互相独立，可以调换顺序，哪边着急先做哪边。

---

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| 找不到 node | 启动脚本依次探测：`which node` → `/usr/local/bin/node` → `~/.workbuddy/binaries/node/versions/*/bin/node`；一个都没有就打印人话提示，不要静默失败 |
| 端口被占用 | 依次尝试 41873 → 41874 → 41875 → 41876；全占则明确报错退出，不偷偷换端口 |
| 中文路径 | `.command` 中不写死任何中文路径，统一用 `cd "$(dirname "$0")"`；脚本本身存为 UTF-8 无 BOM |
| 数据文件被改坏 | 启动时校验；坏了给提示 + 从 `.bak` 恢复 + 用空白数据继续；写入永远走 `.tmp` → 替换 |
| 写盘写到一半断电 | 原子写入保证主文件要么是旧的完整版、要么是新的完整版 |
| 浏览器缓存旧代码 | 静态资源响应头 `no-cache`；开发期按 `Cmd+Shift+R` 强刷 |
| 快速连续改动丢数据 | 300 ms 防抖 + 写入队列，不并发 PUT |
| 日期跨时区错位 | 全部用本地时间拼 `YYYY-MM-DD`，不经过 UTC |
| Safari 兼容（macOS 默认浏览器） | 只用 ES2020 以内语法；避开 `:has()`、容器查询等新特性；拖动用原生 `draggable` |
| 拖动在触控板上不顺手 | 拖动操作**必须有非拖动的备用路径**（如菜单里选「移到下一阶段」），拖动只是快捷方式 |

---

## 7. 明确不做的技术选择（防止跑偏）

- ❌ 不引入任何前端框架、打包器、转译器
- ❌ 不执行 `npm install`，不产生 `node_modules`
- ❌ 不用 localStorage / IndexedDB / Service Worker / PWA
- ❌ 不引入图表库，趋势图用手写 SVG
- ❌ 不写自动化测试框架。项目规模小，PRD §10 的人工验收清单比测试框架更划算
  （例外：`server/selftest.js` 这个数据层自检脚本值得写，因为数据损坏是最贵的失败）
- ❌ 不做开机自启、后台常驻、系统托盘

---

## 8. 交付节奏

每批完成后我会停下来，告诉你「这批做完了，可以打开看」，由你实际点一遍再决定是否继续。**不攒着一次交付六个模块**——万一配色或者交互的手感不对，早发现早改，返工成本差很多。

需要调整批次顺序、砍掉某个模块、或者把某批拆得更细，直接说。

---

## 9. 施工完成情况（2026-09-15 更新）

七个批次全部完成。代码在 `工作台/`，可双击 `工作台/启动工作台.command` 使用。

### 与原计划不一致的地方（都是有意改的）

| 项 | 原计划 | 实际做法 | 为什么 |
|---|---|---|---|
| 自动化测试 | 「不写测试框架，用人工验收清单代替」 | 用 Node 内置的 `node:test`，写了 359 项测试，每批跑一次 | 后来明确要求每阶段加测试。仍然零依赖，没有引入任何测试库 |
| 服务端接口 | 批次 5 才加「打开数据文件夹」 | 批次 0 就把全部接口写完 | 服务端就一个文件，分两次改反而更容易出错 |
| 数据损坏的恢复界面 | 批次 5 | 批次 1 就做了 | 属于「外壳」的一部分：载入失败时连外壳都渲染不出来，必须在外壳之前就能处理 |
| 图标 | 做一个 `icons.svg` 图标精灵 | 没做，界面里不用图标 | 界面里的符号只有 ↑↓‹›→ 这类字符，做图标精灵反而多一次请求 |
| 占位视图模板 | 九个 view 先写骨架 | 骨架在批次 5 全部替换完后删掉了 | 留着一个没人引用的文件是负担 |

### 人工验收清单（这几条我这边没法自动验，需要你双击试一下）

| 条目 | 为什么验不了 |
|---|---|
| 双击脚本后浏览器自动打开 | 脚本启动服务这一段实测通过（HTTP 200、数据目录自动重建），但「自动调起浏览器」这一步没在真机上跑 |
| 刷新后停在当前模块、不回首页 | 路由解析逻辑有测试覆盖，但「刷新」这个动作本身需要真浏览器 |
| 断网后一切正常 | 只能证明代码里没有任何对外请求（静态检查 + 服务端无外部调用），真拔网线没试过 |
| 拖动任务/卡片的顺手程度 | 拖动依赖真实指针事件 |
| 配色和留白好不好看 | 这是主观的 |

### 回归方式

```bash
cd 工作台
node --test 'tests/**/*.test.js'   # 359 项功能测试
node server/selftest.js            # 9 项数据层自检（含损坏恢复演练）
```

