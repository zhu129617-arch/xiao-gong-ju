# ── macOS 系统文件 ──
.DS_Store
._*
.Spotlight-V100
.Trashes

# ── 工作台的实时数据 ──
# 这是你每天在用的个人数据（任务、备忘、健康记录、客户信息），
# 每次输入都会变，而且程序自带备份机制（data.json.bak + 快照 + 导入前备份）。
# 所以不放进版本库，避免每次提交都塞进一份私人数据。
# 如果你反而想连数据一起做版本历史，把这行删掉即可。
工作台/data/

# ── 运行时产物与临时文件 ──
node_modules/
*.log
*.tmp
*.pid

# ── 外观预览页（生成出来的，不进版本库）──
# 它是用 `node 生成外观预览.mjs` 从样式表与视图里现算出来的，
# 属于派生产物，改了样式就会变。生成器本身在库里，随时可以重跑。
工作台/外观预览.html
